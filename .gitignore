# macOS
.DS_Store

# secretos locales (nunca al repo)
.secrets/
.secrets/

# Seguridad: no publicar en el repo (es publico)
docs/auditoria-seguridad.md
docs/auditoria-seguridad-anonimo.md
docs/pendiente-seguridad.md
supabase/.temp/

# CSV generados por export_feedback.py (datos reales, no versionar)
herramientas/export/

# Artefacto efímero del workflow de ping (se genera en CI, no versionar)
cuerpo.json

# Maquetas locales — prototipos de diseño, no commitear (ver CLAUDE.md)
maquetas/
_maqueta*
